package lms_126

fun task9(): String {

    return """
        INPUT master card to machine
        INPUT the correct code 
        IF the cod correct
          INPUT the amount to withdraws
          IF the withdraws more than the balance 
              PRINT try another amount 
          ELSE draw the cash
          OUTPUT the card
          OUTPUT the receipt
        ELSE 
        INPUT try again or remove the card
        
    """
}
