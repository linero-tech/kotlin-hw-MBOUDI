package lms_126

fun task11(): String {

    return """
        INPUT a temperature value and its unit
        store the input in a variable
        EXTRACT the temperature value from the input
        IF the unit is C convert the temperature to F
        IF the unit is F convert the temperature to C
        IF the unit is neither C nor F print error
        OUTPUT the result of the conversion
        
        """


}
